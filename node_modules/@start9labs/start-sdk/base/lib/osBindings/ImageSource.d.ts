import type { BuildArg } from './BuildArg';
export type ImageSource = 'packed' | {
    dockerBuild: {
        workdir?: string;
        dockerfile?: string;
        buildArgs?: {
            [key: string]: BuildArg;
        };
    };
} | {
    dockerTag: string;
};
