import type { HostnameMetadata } from './HostnameMetadata';
export type HostnameInfo = {
    ssl: boolean;
    public: boolean;
    hostname: string;
    port: number | null;
    metadata: HostnameMetadata;
};
