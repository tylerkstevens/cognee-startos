import type { PackageBackupInfo } from './PackageBackupInfo';
import type { PackageId } from './PackageId';
export type BackupInfo = {
    version: string;
    timestamp: string | null;
    packageBackups: {
        [key: PackageId]: PackageBackupInfo;
    };
};
